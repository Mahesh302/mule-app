version: 0.2

phases:
  install:
    commands:
      - echo Installing...
  pre_build:
    commands:
      - echo Pre-build phase...
  build:
    commands:
       - chmod +x ci_build.sh
       - ./ci_build.sh
